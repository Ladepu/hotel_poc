# Hotel Booking Fullstack

Vite + React + Tailwind frontend integrated with FastAPI backend and PostgreSQL.

## Quick start

1. Build and start:

```bash
docker compose up --build
```

2. Frontend: http://localhost:5173
   Backend docs: http://localhost:8000/docs

The backend runs init_db.sh on startup to create tables and seed sample hotels.
